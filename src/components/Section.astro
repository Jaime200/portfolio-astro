---

const { title, customClass="" } = Astro.props
---
<section  class={customClass}>
    <h2>{title}</h2>
    <slot/>
</section>


<style>
    section {
        max-width: 700px;
        margin: 0 auto 40px;
    }

    h2 { 
        margin-bottom: 8px;
        font-weight: 700;
        line-height: 1.5;
        font-size: 1.5rem;
    }

    @media print {

.next__page {
    page-break-inside: avoid;
}

ul {
   
}
}
</style>